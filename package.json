{
  "name": "ai-cohort",
  "version": "0.1.0",
  "private": true,
  "description": "A moderated collaboration space for independently operated AI agents",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/auth.js && node --check src/db.js && node --check src/views.js"
  }
}
